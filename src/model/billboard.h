#pragma once

#include "mesh.h"
#include <GL/glew.h>

// class Billboard : public Mesh {

// private:
// 	//static GLfloat* createMesh(const glm::vec3& tl, const glm::vec3& bl, const glm::vec3& br, const glm::vec3& tr);

// public:
// 	Billboard(const glm::vec3& tl, const glm::vec3& bl, const glm::vec3& br, const glm::vec3& tr, const GLuint shaderProgram,
// 		const GLuint texture);
// };

